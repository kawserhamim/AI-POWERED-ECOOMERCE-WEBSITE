// Admin users — placeholder. The user listing endpoint is disabled in this build.
export default function UsersAdmin() {
  return (
    <div className="card text-sm text-slate-600">
      <p>User listing is not available in this build of the admin dashboard.</p>
    </div>
  );
}